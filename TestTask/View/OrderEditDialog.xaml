<Window x:Class="TestTask.View.OrderEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заказ" Height="280" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Дата:" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="0" Grid.Column="1" x:Name="DpDate" Margin="0,4"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Сумма:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtAmount" Margin="0,4" Padding="4"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Сотрудник:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="CmbEmployee" Margin="0,4"
                  DisplayMemberPath="FullName"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Контрагент:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="CmbContractor" Margin="0,4"
                  DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Сохранить" Width="90" Margin="0,0,8,0" Click="SaveClick"/>
            <Button Content="Отмена" Width="90" Click="CancelClick"/>
        </StackPanel>
    </Grid>
</Window>
